# 用户系统需求分析文档

**产品工作名：** 关卡学  
**文档性质：** 在核心 MVP（粘贴文本 → 出题 → 答题 → 报告）已经可跑通之后，追加「用户系统及用户相关能力」的需求基线。  
**文档版本：** v0.3（待人工确认）  
**撰写日期：** 2026-08-19  
**依据：** [需求分析文档.md](./需求分析文档.md) v1.0、[方案设计文档.md](./方案设计文档.md) v1.2、[ui-prototype/01-core-flow.html](./ui-prototype/01-core-flow.html)、[ui-prototype/02-extended.html](./ui-prototype/02-extended.html)  
**已口头确认（本轮对话）：** 登录走微信小程序；允许新增登录/资料页，视觉跟现有暖色软 UI；**微信静默登录 + JWT 鉴权 + MySQL 后端存储**（见 3.1）；先不要写完整方案，本文确认后再往下。

**决策结论（先看这个）：用户系统按批次做，不要一次上完整学习后台。** 第一批用**微信静默登录**认出同一个人（无头像昵称弹窗），后续请求用 **JWT 鉴权**，用户与闯关记录落在 **MySQL**；第二批才在用户同意后展示头像昵称；第三批才出现「打过的关」。小程序内打开即有身份，不再做「点登录」墙。H5 仍无微信登录，可继续无账号试打。不做能力雷达、学时竞赛、VIP、手机号强绑。

---

## 1. 文档说明

### 1.1 目标读者

给产品决策和后续方案/开发当需求基线。读完应能回答：这一轮用户相关功能分几批、每批用户能感知到什么、什么明确不做。

### 1.2 范围

本文覆盖：为何现在做用户系统、与总需文档的对齐、用户与场景、**需求批次与优先级**、每批的使用方式与成功标准、边界、待确认项。

### 1.3 明确不含

- 完整技术方案、系统架构、接口字段、数据库表、云开发选型（这些等本文确认后另写方案）
- 页面线框之外的工程实施计划、人天排期
- 链接解析、来源确认、多视角题、分享海报工坊（见扩展原型第一行，不属于「用户系统」本轮主线）

文中的「登录」「记录」「我的」均指用户可感知的能力。  
**例外：** 提出人已指定后续方案必须采用 **微信静默登录 + JWT 鉴权 + MySQL 后端存储**。本文只把这三条写成需求约束，不展开表结构、签发算法、连接池等实现细节。

### 1.4 本文怎么来的

提出人要求：功能多，要**分需求批次、排优先级**；举例顺序为「先用户登录 → 再获取用户信息 → 再闯关历史列表等」。

截至撰写时，提出人未另贴一份独立想法清单。本文把该顺序落到总需里的 **U7 / 8.6 极简学习记录**，以及扩展原型里已经画出来的 **「打过的关」「关于题目」**，并标出仍需你拍板的点（见第 9 节）。若你后续补想法，以补丁修订本文，不直接改总需文档。

### 1.5 已锁定的实现约束（给后续方案用，本文不展开）

提出人指定，后续方案与开发必须遵守，不得改口成 SQLite / 无库 / Session Cookie 等替代：

| 约束 | 用户侧含义 |
| --- | --- |
| 微信静默登录 | 小程序打开即可认出同一微信用户；无头像昵称弹窗、无登录墙 |
| JWT 鉴权 | 静默成功后，需要「是谁」的请求都带这份凭证；过期则再静默 |
| MySQL 后端存储 | 用户身份与闯关记录以 MySQL 为权威，不能只放在手机内存或本地文件 |

表怎么拆、JWT 里放什么字段、微信 `code` 怎么换，等方案文档再写。

---

## 2. 为何现在做、解决什么

核心四步已经能证明「指定材料可以打完并出报告」。总需里的 **U7（P1）** 是：回头看到自己打过哪些关、必要时再打一次。没有稳定身份，记录只能活在这一次打开里，刷新即丢——这是当前 H5 占位页已经写明的现状。

用户系统在本产品里**不是**为了做成学习管理系统，而是为了三件小事：

1. **认出同一个人**（换设备或第二天回来，仍是「我」）。
2. **把通关结果留下来**，列表能打开，而不是每次从零粘贴。
3. **「我的」不再是空壳**，能看见自己是谁，以及题目由 AI 生成的说明。

它不解决：获客（分享仍应能不登录开打）、变现（VIP 仍是 P2）、应试打卡。

---

## 3. 与总需文档、原型的对齐

| 条目 | 总需 / 原型怎么说 | 本文怎么收 |
| --- | --- | --- |
| U7 学习记录 | P1；列表 + 正确率 + 再打 | 放到 **批次 3**，登录之后才有权威列表 |
| 8.1 输入 | 默认不要求账号即可试一次 | **小程序：静默登录，无弹窗、无登录墙，打开就能打关。** H5：仍无账号试打 |
| 8.6 轻后台 | 主题、日期、正确率、再打；不要雷达和学时竞赛 | 列表字段对齐扩展原型；竞赛类明确不做 |
| 8.6 「本周复习」 | 可生成一页，只列出错过的点 | **批次 5**，本轮确认后仍默认不做 |
| 支线 C 再打 | 原题再打，或针对错题出更短关 | **原题再打 = 批次 4**；错题短关 = 批次 5 |
| 扩展原型「打过的关」 | 标题、相对时间、4/6、再打；底栏记录高亮 | **批次 3 的界面必须按此**，不自行发挥列表形态 |
| 扩展原型「关于题目」 | AI 生成、有依据才出题；不是题库/家教/商城 | **批次 2**：从「我的」进入；文案按原型 |
| 扩展原型「分享落地」 | 不必先注册就能开始答题 | 分享开打 **不** 纳入本轮用户系统主线；原则保留 |
| 核心原型首页「未完成的关卡」 | 进度环、还差 N 题（现为静态展示） | **批次 4**：有身份之后才变成真数据 |
| 核心原型底栏 | 关卡 / 记录 / 我的 | 批次 1 起三栏都可进入对应页，不再用空壳搪塞 |
| 第一期方案 | 无登录、无库 | 本文是加项：**静默登录 + JWT + MySQL**，不回溯改写出题协议 |

---

## 4. 用户与场景

### 4.1 谁会碰到用户系统

仍是泛知识学习者。他们不会为「注册关卡学」专程来；他们是打完一关之后，希望明天还能找到这一关。

### 4.2 典型场景（按批次递进）

| 场景 | 用户在做什么 | 本轮哪一批才算做成 |
| --- | --- | --- |
| A. 微信里打开小程序 | 无弹窗即被认出；直接打关 | 批次 1 |
| B. 打开「我的」，看到自己的称呼 | 确认「这是我的号」，并看到 AI 生成说明 | 批次 2 |
| C. 昨天打完「行为经济学」，今天在「记录」里看到 4/6，点再打 | 回访，而不是重新粘贴原文 | 批次 3 + 4 |
| D. 首页看到「还差 3 题」的未完成关 | 中途退出后还能续打 | 批次 4 |
| E. 只复习错过的点 | 更短的一关，不是仪表盘 | 批次 5，默认可不做 |

### 4.3 静默登录之后，还有没有「未登录」

**微信小程序（本轮用户系统主场）：**  
静默登录 = 进入小程序即可完成身份识别，**不弹**头像/昵称授权窗，**不必**先点「登录」才能打关。因此小程序里不再维护一套「游客」。通关记录从该微信用户第一次打完就开始留（批次 3）。

**H5：** 本轮不做微信网页登录。H5 仍按现在 MVP：无账号可打关，记录 Tab 为空态。

**「退出」在静默登录下几乎立刻失效：** 清掉本地 JWT 后，下次打开仍会再次静默登录，还是同一个人，记录还在。若仍做退出按钮，只表示「本机暂时不带票」；默认 **批次 1 不做退出**，见第 9 节 Q10。

---

## 5. 需求批次与优先级

优先级只服务「用户能感知到的闭环」，不服务技术分层。一批做完、能验收，再开下一批。

| 批次 | 名称 | 优先级 | 用户可感知的最小闭环 | 依赖 |
| --- | --- | --- | --- | --- |
| **1** | 认出我 | **P0 · 先做** | 打开小程序即静默登录；打关不拦；后续请求 JWT 鉴权 | 微信小程序身份；MySQL 能记下用户 |
| **2** | 看见我 | **P0 · 紧随** | 「我的」展示头像/昵称（或明确的未授权态）；可打开「关于题目」 | 批次 1 |
| **3** | 留下关 | **P0 · 用户系统真正有用** | 登录用户打完一关后，「打过的关」出现一条；未登录列表为空 | 批次 1；通关报告已存在 |
| **4** | 再打开 | **P1** | 列表「再打」进入原关；首页未完成关变成该用户的真数据 | 批次 3 |
| **5** | 更轻的复习 | **P2 · 本轮默认不做** | 错题短关、本周复习一页 | 批次 4 |

**建议本轮人工确认后的开发范围：批次 1 + 2 + 3。**  
批次 4 可在 1–3 验收后再单开。批次 5 不进本轮，除非你明确加进来。

下面第 6 节按批次写清「做什么 / 不做什么」。批次 4、5 写进文档是为了排队，不等于现在开工。

---

## 6. 分批功能说明

每条包含：解决什么、用户怎么用、成功标准、边界。界面凡扩展/核心原型已有画的，**按原型**；原型没有的登录/资料，允许新建页，但必须同一套暖色软 UI（大圆角卡片、橙色胶囊按钮），不得改成通用后台风。

### 批次 1 — 认出我（P0）

**解决什么：** 没有稳定身份，后面所有「我的记录」都无法成立。

**包括：**

1. **微信静默登录。** 进入小程序即可完成识别：无头像、无昵称弹窗，无全屏登录墙。用户感知是「打开就能用」，不是「先注册」。
2. **后续请求 JWT 鉴权。** 静默登录成功后，打关、出报告、拉记录等需要认出「谁」的操作都带 JWT。过期或失败时再次静默登录，而不是把用户拦在门外。
3. **身份与记录的权威在 MySQL。** 同一微信用户再次打开，仍应对上同一条用户；通关记录（批次 3）从库里读，不依赖这次打开是否还在内存里。
3. **打开就能打关。** 首页生成、等待、答题、报告与现在 MVP 一致，不插入「请先登录」。
4. **同一微信用户再次打开，仍是同一个人**（直到微信侧身份失效，再静默一次即可）。
5. **失败可理解。** 静默登录因网络/微信失败时，打关可以先走现有无账号路径（与 H5 类似），并在「我的」用短句说明「还没连上」；成功后再把之后的关记到该用户。批次 3 之前未连上时不写记录。

**成功标准：**

- 同一微信用户两次打开小程序，无需点登录，仍被当作同一人。
- 打开后可以直接打完一套关并看到报告，中间没有授权弹窗。
- 「我的」不再写「第一期没有账号」；未授权资料时可用「同学」，但应能看出已连上微信（或明确「还没连上」）。

**边界：**

- 不做手机号、邮箱、用户名密码、验证码登录。
- 不做一打开就全屏拦截的登录墙，也不做「点击登录」才发静默请求的假门槛（静默应在进入时完成）。
- 不做多账号切换、注销账号。
- 本批 **不** 拉头像昵称（那是批次 2）。
- H5 不做微信网页登录 / 网页 JWT 对等方案。
- **退出登录**默认不做（静默下清票后下次打开又会登回同一人）。若你要退出，见 Q10。

### 批次 2 — 看见我（P0）

**解决什么：** 登录若只是一个看不见的身份，用户无法确认「这是我」；「我的」也需要对齐核心原型的问候区。

**包括：**

1. **获取并展示用户信息。** 在用户明确同意后，展示微信昵称、头像；「我的」问候从「你好，同学」可改为该昵称（未授权时仍可用「同学」）。
2. **拒绝授权仍保持登录。** 用户可以已登录但没有头像昵称；列表（批次 3）仍按身份存，不因为拒资料而退回未登录。
3. **关于题目。** 从「我的」进入扩展原型「说明」页，文案按原型：  
   - 标题：关于题目  
   - 「AI 生成，有依据才出题。」  
   - 「材料由你提供。题目、讲解、报告由模型生成，可能不准确。每题应能指回原句。」  
   - 「不是题库、不是家教、不是课程商城。」  
4. **资料授权与静默登录分离。** 拒绝头像昵称不影响身份和记录。若做「退出」，不得做成必须重新弹资料窗才能再打关。

**成功标准：**

- 授权后「我的」能看到昵称和头像。
- 未授权时页面仍完整，不出现破图或空白崩溃。
- 「关于题目」与扩展原型该屏的信息结构一致。

**边界：**

- 不收集性别、地区、手机号、通讯录。
- 不做个人资料编辑器、个性签名、装扮。
- 不做会员标识、金币、等级。

### 批次 3 — 留下关（P0，建议与 1、2 同轮交付）

**解决什么：** 这是用户系统对学习的唯一直接价值——打过的关还能被找到。对应总需 U7 / 8.6 的最小切片，界面以扩展原型「打过的关」为准。

**包括：**

1. **登录用户通关后写入一条记录。** 通关 = 打完并生成报告。字段用户能看见的：关卡标题（如「行为经济学」）、时间（如「昨天」）、选择正确率（如「4/6」）。短答不计入对错题数（与现有报告规则一致）。
2. **记录 Tab 列表。** 按扩展原型：顶栏标题「打过的关」；每条卡片左侧标题+时间与分数，右侧「再打」按钮（本批按钮可以先进入该关只读/提示「下一批才能再打」，**待确认**；默认建议本批「再打」先能打开该关或报告，完整原题再打放到批次 4）。
3. **空态。** 静默登录成功但还没通关过：列表为空，一句轻提示，**不要**能力雷达空图表。H5 无账号时同样空态。
4. **只显示当前这个微信用户自己的关。** 不能看到别人的列表。
5. **底栏。** 在记录页「记录」为选中态，与原型一致。

**成功标准：**

- 小程序静默登录成功后打完一关，回到「记录」能看到刚打的那一条，标题可识别，分数与报告里的选择对错一致。
- H5 无账号打完，记录页仍为空。
- 列表没有学时、连续打卡、排行、雷达。
- 抽查：杀掉小程序再打开（未清微信登录态），记录还在（来自 MySQL，不是当次内存）。

**边界：**

- 不做搜索、文件夹、标签、多选删除。删除单条：**待确认**，默认本批可不做。
- 不做「本周复习」汇总页（批次 5）。
- 不把完整材料原文做成用户可对外转载的文库。
- 首页静态的「未完成的关卡」「快速主题」本批仍可保持展示，不强制改成真数据。

**「再打」在批次 3 的深度（待确认，见第 9 节）：**

- 方案 A：本批「再打」仅占位或打开上次报告。  
- 方案 B：本批「再打」已能进入原题开打（等于把批次 4 的一部分提前）。  
默认写进确认清单的是 **方案 A**，以便批次 3 能先把「留下」做稳。

### 批次 4 — 再打开（P1，建议下一批）

**解决什么：** 记录如果不能再打，只是一份成绩单。

**包括：**

1. **原题再打。** 从「打过的关」点「再打」，打的是同一套题（同一材料生成的那一份关卡），而不是按标题重新让模型出一套新题。
2. **未完成关卡。** 核心原型首页的进度条列表，改为当前用户真实的未打完关；未登录仍可用现在的静态示意或隐藏该块（**待确认**）。
3. **中途退出。** 答题中途离开，记录为未完成，而不是假通关。

**成功标准：** 回访用户能从记录一键开打上次那一关；题目与上次同一套。

**边界：** 不自动生成「错题加强包」（那是批次 5）。不做 PK。

### 批次 5 — 更轻的复习（P2，本轮默认不做）

总需 8.6 的「本周复习」和支线 C 的「针对错题出一组更短的关」。有价值，但会重新碰模型出题与题库切片，容易拖住 1–3 的验收。

**边界提醒：** 仍然不要能力雷达、知识图谱、学时竞赛。

---

## 7. 页面与文案约束（需求级，不是视觉规范书）

1. **已有原型的屏，按原型做。** 批次 3 的「打过的关」、批次 2 的「关于题目」以 `02-extended.html` 为准。底栏关卡/记录/我的以 `01-core-flow.html` 为准。
2. **原型没有的登录/授权/退出，允许新页或嵌在「我的」卡片里**，但颜色、圆角、主按钮必须跟现有暖色软 UI，禁止另做一套设置页风格。
3. **禁止**把「我的」做成：订单、优惠券、每日签到、积分商城。
4. 生成内容仍需可识别为 AI 生成；「关于题目」承担主要说明，关卡页已有的标识不因为登录而删掉。
5. 列表时间可用相对时间（昨天、3 月前），与原型示例一致。

---

## 8. 明确不做（本用户系统范围内）

即使以后要做，也不进批次 1–3：

1. 登录墙、试打次数用完强制注册  
2. 手机号、短信、邮箱密码、第三方非微信登录  
3. VIP、次数包、支付  
4. 能力雷达、知识图谱、学时、连续打卡、排行榜、徽章  
5. 社交资料、关注、评论、班级  
6. 企业组织账号、管理员后台  
7. 注销与数据可携的完整合规套件（需单独需求；本轮默认不做退出、也不做注销）  
8. 链接输入、来源确认、多视角题、分享海报（扩展原型有画，但是另一条产品线）  
9. 为做用户系统而改变出题规则、题型或报告结构  

---

## 9. 待你确认的点（确认前不当作已定需求）

请在确认本文时一并拍板。未拍板的，开发不得按「猜的」做。

| ID | 问题 | 本文默认（可改） |
| --- | --- | --- |
| Q1 | 本轮开发范围是否就是 **批次 1+2+3**，4 和 5 以后再开？ | 是 |
| Q2 | 小程序是否还要「游客 / 未登录也能打关」？ | **改为：打开即静默登录，无游客。** 打关不拦；连不上微信时才临时无身份 |
| Q3 | 有没有「登录前试打、登录后合并」？ | **不再适用**（无游客）。H5 打的关不合并进微信账号 |
| Q4 | H5 本轮要不要做微信网页登录？ | 不做；只做小程序静默登录 + JWT 鉴权 + MySQL |
| Q11 | 用户与记录是否必须进 MySQL（不能只放本地或 SQLite 文件）？ | **必须 MySQL** |
| Q10 | 静默登录还要不要「退出」？ | **不要。** 清 JWT 后下次打开仍是同一人 |
| Q5 | 批次 3 的「再打」做到哪一步？ | 方案 A：先能留下列表；再打的完整体验放批次 4 |
| Q6 | 首页「未完成的关卡」何时改成真数据？ | 批次 3 仍用原型静态示意；批次 4 再改 |
| Q7 | 是否需要删除单条记录？ | 本轮不要 |
| Q8 | 微信小程序 AppID / 测试号现在有没有？ | **【待你填写】** 没有则批次 1 只能做到「界面与流程」，真登录联调会卡住 |
| Q9 | 你是否还有未写入本文的个人想法（资料页字段、要不要游客转正等）？ | 有则补进修订，无则按本文 |

---

## 10. 批次 1–3 的产品验收（确认开发后用）

做完 1+2+3，应能当着你的面走完：

1. 打开小程序：无登录墙、无资料弹窗，可直接贴材料打完并出报告。  
2. 再进小程序仍是同一人，无需再点登录。  
3. （可选）授权资料后，「我的」出现昵称/头像；不授权也不丢身份、不丢记录。  
4. 打开「关于题目」，能看到原型上的三句说明。  
5. 打完一关 → 「打过的关」多一条，标题和 4/6 这类分数对得上。  
6. 全程没有雷达、签到、强制登录墙、头像授权强弹。

---

## 11. 确认之后才允许的下一步（本文不展开）

你确认本文（含第 9 节选项）之后，才进入：

1. 用户系统的 **方案设计**（须遵守：微信静默登录、JWT 鉴权、MySQL；接口与表结构用当时官方文档 / Context7 核对）  
2. 方案再经你确认  
3. 按批次开发；后端 TDD；UI 有原型的屏严格按原型  

**现在不要开始写代码，也不要在本文里夹带技术选型。**

---

## 12. 修订记录

| 版本 | 日期 | 说明 |
| --- | --- | --- |
| v0.1 | 2026-08-19 | 初稿。分 5 批；建议本轮做 1–3。待人工确认。 |
| v0.2 | 2026-08-19 | 提出人指定 **微信静默登录 + JWT**。小程序取消游客与「点登录」；H5 仍无账号。默认不做退出。 |
| v0.3 | 2026-08-19 | 追加 **JWT 鉴权** 表述与 **MySQL 后端存储** 为硬约束。 |
